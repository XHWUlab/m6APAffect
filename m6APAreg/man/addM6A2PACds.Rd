% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m6APAreg_funclib.R
\name{addM6A2PACds}
\alias{addM6A2PACds}
\title{Add the nearest upstream m6A site to each polyadenylation site.}
\usage{
addM6A2PACds(
  pacds,
  m6ads,
  peakcols = c("peak_start", "peak_end"),
  label = "m6A_",
  d = 2000,
  mcols = NULL,
  verbose = TRUE
)
}
\arguments{
\item{pacds}{A PACdataset object containing polyadenylation sites.}

\item{m6ads}{A PACdataset object containing m6A sites.}

\item{peakcols}{A character vector of length two specifying the column
names in `m6ads` that contain the m6A start and end coordinates.
The default is `c("peak_start", "peak_en")`.}

\item{label}{A character string specifying the prefix of the output
columns. The default is `"m6A_"`. For example, the function may
create columns such as `m6A_ovp`, `m6A_dist`, and columns derived
from `mcols`.}

\item{d}{A numeric value specifying the upstream distance used to
extend the polyadenylation site region when determining overlapping
m6A sites. The default is 2000.}

\item{mcols}{A character vector of additional metadata column names
to add to `pacds@anno`. These columns use the prefix specified by
`label`.}

\item{verbose}{Logical value indicating whether progress messages
should be shown. The default is `TRUE`.}
}
\value{
A PACdataset containing the added m6A overlap, distance, and
  metadata columns.
}
\description{
`addM6A2PACds()` adds the nearest upstream m6A site to each
polyadenylation site in a PACdataset.
}
\details{
The function searches for the nearest upstream m6A site of each
polyadenylation site and records the distance and m6A coordinate.
It adds columns with the prefix specified by `label` to
`pacds@anno`, including overlap and distance information.
}
\examples{
\dontrun{
p1 <- findOvpPACds(
  qryPACds = pdAPA,
  sbjPACds = m6ads,
  qryMode = "point",
  sbjMode = "region",
  d = 1000
)

p2 <- annotateByKnownPAC(
  pdAPA,
  knownPACdss = m6ads,
  labels = "m6A",
  d = 1000,
  verbose = TRUE
)

p3 <- addM6A2PACds(
  pdAPA,
  m6ads,
  peakcols = c("peak_start", "peak_end"),
  label = "m6A_",
  d = 1000,
  mcols = c("score", "coord", "gene")
)
}
}
\seealso{
Other APAds functions:
\code{\link[=APAds2GLMds]{APAds2GLMds()}},
\code{\link[=QAPA2GLMds]{QAPA2GLMds()}},
\code{\link[=addMMPAS2PACds]{addMMPAS2PACds()}},
\code{\link[=addPAS2PACds]{addPAS2PACds()}},
\code{\link[=get3UTRAPApd]{get3UTRAPApd()}},
\code{\link[=get3UTRAPApdDE]{get3UTRAPApdDE()}},
\code{\link[=mergePACdsCoords]{mergePACdsCoords()}},
\code{\link[=mergePACdsRanges]{mergePACdsRanges()}}
}
\concept{APAds functions}
